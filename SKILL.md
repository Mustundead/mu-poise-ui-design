---
name: mu-poise-ui-design
description: 设计、实现和评审细致、可控的产品界面与交互。用于布局层级、排版、控件一致性、拖拽与弹层、可中断动效、材质和无障碍；遵循现有原生或 Web 平台，不以统一玻璃风格替代产品设计。
---

# MU Poise · UI 设计

MU LABS / Mustundead

让界面看得清、操作跟得上、变化有来由。细节应帮助人理解和掌控产品。

这是 Mustundead 的界面设计方法：从影响真实使用的小问题入手，把视觉关系、行为和运行证据放在一起判断。借鉴流畅交互的通用原则，保留产品自己的形态；不模拟某个操作系统的外观来代替设计。

## 先判断要交付什么

| 请求 | 范围与结果 |
| --- | --- |
| 修改间距、对齐或控件 | 找到拥有问题的布局层，修正该处与受影响的共享组件，保留操作契约。 |
| 实现已有设计 | 以用户选定的参考和项目规范为准；在原有框架中完成行为与状态。 |
| 新流程或明显重设计 | 先说清任务路径、数据后果和关键状态，再做可交互实现；需要时查当前平台资料。 |
| 评审设计 | 给出能定位、能解释影响的发现；未经要求不改代码。 |
| 探索动效 | 交付约定的演示或产品交互，不顺带换技术栈或建立新设计系统。 |

原生应用留在原生平台，浏览器界面留在当前前端栈。用户指定设备或运行版本时，该表面才是验收对象。布局修正不需要重新做一轮品牌发现。

## 先写一个可观察的结果

检查当前组件、设计变量、可用截图或运行界面、内容所有者与相关测试。简短记录：

`用户任务 → 触发动作 → 状态变化 → 可见反馈 → 数据影响 → 验收方式`

例：“拖动 ByTouch 配料面板时跟随手指，松开后停在合法位置；反向拖动不中断控制；减少动态效果时保留明确状态变化；菜谱数据不变。”这是教学假设，实施前确认项目真实行为。

复用当前授权和已有证据，不另设设计批准关卡。无法确定的事实会影响后果时再问；可逆的视觉选择由项目风格支持。

## MU LABS 的四个判断

### 1. 看得懂：关系先于装饰

页面应能让人知道当前位置、主要任务和退出路径。用内容顺序、间距、字号与字重建立层级；不要把所有内容塞进同一个最简容器。

对齐比较的是可见边缘和阅读关系。父容器内边距、控件固有尺寸、字形边界、图像透明边缘是不同原因；在拥有问题的层修正。控件看起来变小，不代表点击范围也应缩小。

字体优先沿用项目或平台选择。中文不机械套用英文负字距；数字、单位、长标签和不同脚本都要检查。文本放大后布局应容纳内容，不用缩字和裁切掩盖问题。

### 2. 跟得上：输入与反馈连续

按下时及时提供视觉反馈；有意义的提交发生在有效释放或键盘激活后。拖动过程中持续反馈，不等结束后再播放动画。保留取消、滚动和键盘等价操作。

可被重新抓取的运动应从当前显示位置继续，避免跳回旧目标。释放速度、目标选择和边界规则共同决定落点；先保证正确、可取消，再调节手感。

弹簧适合有连续目标变化的运动；简单悬停、颜色或透明度变化可用 CSS。不为了“流畅”引入新动画库，也不把某套阻尼/响应参数直接当成另一套 API 的单位。

涉及手势、中断、速度、滚动冲突时阅读 [交互与动效](references/interaction-and-motion.md)。

### 3. 有分寸：材质和动效服务内容

材质用于说明层级或内容关系。大面积正文、表格和复杂背景可以优先使用稳定的实色表面。模糊不是对比度保证，也不是所有导航、按钮、弹层的默认要求。

动效解释来源、状态与目的地。回退路径通常保留空间关系，但实际语义比机械对称更重要。轻微弹性只有在操作含义与产品气质支持时使用。不要强制进场弹跳、全屏视差、光晕或循环动画。

遵循已有的明暗主题、无障碍对比、减少透明度和减少动态效果设置。必要时用静态状态或短淡变表达同样信息，不留下只移除 transform 的残缺交互。

相关布局、材质和排版判断见 [视觉与可访问性](references/visual-and-accessibility.md)。

### 4. 能证明：状态、行为和证据一致

加载、无内容、旧数据、部分成功、失败、无权限、待确认与完成，在相关流程中应各自有真实表现。不要把未知值填成零，不因一个来源失败隐藏其他结果。

视觉调整不能改变来源身份、账户权限、用户数据、权益或通知设置。只有动作本来会改变的数据才能变化；外部发布、交易、删除按实际授权处理。

在当前候选版本上验证。旧截图、构建日志、静态原型都不能替代所要求的运行界面。

## 实现与验证

1. **处理原因。** 在当前组件和框架中做窄范围修改；共享组件改动检查受影响调用，保留用户其他工作。
2. **完成状态。** 把交互的开始、进行、提交、取消和恢复连起来；不要只做成功态外观。
3. **检查实际结果。** 运行受影响的逻辑检查、构建和界面路径，截取有代表性的当前画面。覆盖会改变意义或布局的语言、尺寸、内容和可访问性状态。
4. **验证运动。** 点击、快速重复、拖动反向、松开、取消和减少动态效果按相关性选择。静态截图不能证明中断连续性或帧率；可用实际交互、录屏或性能数据补充。
5. **交付证据。** 简述改了什么、在哪个版本/设备/状态验证，以及剩余限制。仅源码检查时照实说明，不写“已通过真机验收”。

详细验收情境见 [MU LABS 示例与验收](references/examples-and-acceptance.md)。平台行为或 API 不确定时按 [资料与取舍](references/sources.md) 定向查证。

满足本次可观察条件后停止。不给小修正附加无关平台、全产品审计、固定参考数量或主观完美分数。
